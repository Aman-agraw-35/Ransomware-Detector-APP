# Optional overrides for model artifacts
# MODEL_PATH=artifacts_phase2/bilstm_attention_model.keras
# ATTN_MODEL_PATH=artifacts_phase2/bilstm_attention_with_weights.keras
# SCALER_PATH=artifacts_phase2/scaler.pkl
# FEATURE_NAMES_PATH=artifacts_phase2/feature_names.json
# GROUP_CONFIG_PATH=artifacts_phase2/group_config.json
# SHAP_BACKGROUND_PATH=artifacts_phase2/shap_background.npy
# SHAP_TOP_K=15

# Set MODEL_MOCK=1 to skip artifact loading and return deterministic mock results.
# Automatically activated when artifact files are absent.
# Useful for frontend development without trained model files.
# MODEL_MOCK=1
